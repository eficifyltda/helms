{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "metabase.postgresql.name" . }}
  labels:
    {{- include "metabase.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
type: Opaque
data:
  postgres-password: {{ include "metabase.postgresql.password" . | b64enc | quote }}
  postgres-user: {{ .Values.postgresql.username | b64enc | quote }}
  postgres-db: {{ .Values.postgresql.database | b64enc | quote }}
{{- end }}

