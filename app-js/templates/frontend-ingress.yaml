{{- if and .Values.frontend.enabled .Values.ingress.enabled .Values.ingress.frontend.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "app-js.frontend.name" . }}
  labels:
    {{- include "app-js.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.middlewares: default-headers@kubernetescrd
spec:
  ingressClassName: {{ .Values.ingress.className }}
  {{- if .Values.ingress.frontend.tls }}
  tls:
  - hosts:
    - {{ .Values.ingress.frontend.host }}
    secretName: {{ include "app-js.frontend.name" . }}-tls
  {{- end }}
  rules:
  - host: {{ .Values.ingress.frontend.host }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ include "app-js.frontend.name" . }}
            port:
              number: {{ .Values.services.frontend.port }}
{{- end }}

